# Copy values into your shell environment; the application does not write this file.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-terra

# Optional override. Defaults to ./prompts relative to the repository root.
# MPIE_PROMPT_DIR=/absolute/path/to/prompts

